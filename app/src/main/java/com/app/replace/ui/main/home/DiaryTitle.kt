package com.app.replace.ui.main.home

import androidx.annotation.StringRes
import com.app.replace.R

enum class DiaryTitle(@StringRes val title: Int) {
    Our(R.string.our_diary_title), All(R.string.all_diary_title)
}
